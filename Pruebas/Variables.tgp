-- Comentario

@Principal(
 New @bu1(Bool, False);
 New @as(Num,12);
 New @otra(Num,13);
 New @opo3(Num,13);
 Case When (isTrue(@bu1)) Then (
  New @kiki(Bool, True);
  AlterB(@kiki);
  AlterB(@kiki);
  PrintValues(@kiki);
  PrintValues("Hola hola");  
 )
 Case When (isTrue(@bu1)) Then (
  PrintValues("else");
 )
 Else (
  PrintValues("Jeje");
 );
);



















