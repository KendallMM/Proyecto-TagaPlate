-- Comentario

Proc @xyz(
 PrintValues("Este es el procedimiento de arriba");
 PrintValues(@var);
);

@Principal(
 New @counter(Num,100);
 New @var(Num, 0);
 While @var <= @counter (
  CALL(@xyz);
  CALL(@deb21);
  Alter(@var, ADD, 1);
 );
);

Proc @deb21(
 PrintValues("Este es deb");
);























